/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // add any settings you previously had in next.config.ts here
  // images: { domains: ["lh3.googleusercontent.com"] },
};

export default nextConfig;
